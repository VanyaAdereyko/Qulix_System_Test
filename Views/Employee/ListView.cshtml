@{ 
    Layout = "_Layout";
}   

 @*<h3>@Model.employeesCompany</h3>*@
<div class="row mt-5 mb-2"></div>
    @{ 
        foreach (var employee in Model.AllEmployees)
        {
       
            <div class="col-lg-4">
                <h3>Имя: @employee.name</h3>
                <p>Фамилия: @employee.surname</p>
            </div>
        }
    }

